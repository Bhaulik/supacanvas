{
  "name": "supacanvas-share-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker that hosts supacanvas.com — public canvas sharing for the supacanvas CLI.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "compile": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.87.0"
  }
}
